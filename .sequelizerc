const { resolve } = require("path");

module.exports = {
  config: resolve(__dirname, "src","database", "configs", "db.js"),
  "models-path": resolve(__dirname, "src", "api", "models"),
  "migrations-path": resolve(__dirname, "src", "database", "migrations"),
};